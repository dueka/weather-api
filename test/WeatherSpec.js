describe("Weather", function()) {
    it("Returns the current time and weather in degrees for different locations", function() {
        var timeLimit = new Date().toTimeString(),
        var weather = new String('')
        expect.toTimeString(timeLimit);
        expect.toBeString(weather);
    })
} 